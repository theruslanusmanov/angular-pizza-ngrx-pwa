language: node_js
node_js:
  - "11"
dist: trusty
sudo: required
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN # Set in the settings page of your repository, as a secure variable
  keep-history: true
  local-dir: dist/pizza-pwa
  on:
    branch: master
before_script:
  - npm install -g @angular/cli
script:
# - ng lint
# - ng test --watch=false --browsers=ChromeHeadless
- ng build
